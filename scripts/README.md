# Scripts

Convenience launch scripts for training.

## Files

- `train_vae.sh`: starts Stage-I VAE training.
- `train_diffusion.sh`: starts Stage-II diffusion + adapter training.

## Notes

- These scripts are wrappers around the same commands documented in the root `README.md`.
- If your environment path differs, edit config paths before running.
